# Node.js Hello World Helm Chart

Este _chart_ Helm implanta uma aplicação Node.js "Hello World" em um cluster Kubernetes.

## Pré-requisitos

- Kubernetes 1.19+
- Helm 3.0+

## Instalação do Chart

Para instalar o _chart_, use o comando:

```bash
helm install node-hello-world .